[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "blood"
version = "0.1.0"
description = "RF-DETR + Cutie blood segmentation inference for ROS 2"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["blood*"]
